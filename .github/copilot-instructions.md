Read and follow the AGENTS.md file in the repository root — it contains all
project instructions: keep this site plain HTML/CSS with no build step, no
frameworks, no PII, and preserve the per-page image naming convention and
the plain-English docs (EDITING.md, README.md).
